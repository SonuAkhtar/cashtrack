export default function Loading() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        padding: "var(--space-9)",
        color: "var(--text-tertiary)",
        fontSize: "var(--text-sm)",
      }}
    >
      Loading
    </div>
  );
}
