import { themeTokens } from "@/styles/theme";

export const chartPalette = themeTokens.chart;
export const categoryPalette = themeTokens.category;
export const statusPalette = themeTokens.status;

export const chartTooltipStyle = {
  background: "var(--surface-2)",
  border: "1px solid var(--border-default)",
  borderRadius: 12,
  padding: "8px 12px",
  boxShadow: "var(--shadow-md)",
  fontSize: 12,
  color: "var(--text-primary)",
};
